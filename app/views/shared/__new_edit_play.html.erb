<div class="container">
  <%= simple_form_for(@play) do |f| %>
    <%= f.association :game, label_method: :name, value_method: :id, include_blank:false %>
<!--<div class="border rounded ml-2 p-3">
      <h1>New Game</h1>
      <%= simple_form_for(@new_game) do |t| %>
        <%= t.input :name %>
        <%= t.association :company %>
        <%= t.submit "Submit", class: "btn btn-primary" %>
      <% end -%>
    </div> -->
    <%= f.input :done %>
    <%= f.input :won, as: :radio_buttons %>
    <%= f.input :date %>
    <%= f.input :players %>
    <%#= f.input :photos, as: :file, input_html: { multiple: true } %>
    <%= f.submit "Submit", class: "btn btn-primary" %>
  <% end %>
</div>
