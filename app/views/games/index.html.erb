<div class="container">
  <%= link_to "New Game", new_game_path, class:"btn btn-success mb-5" %>

  <ul>
    <% @games.each do |game| %>
      <li>
        <%= link_to game_path(game) do  %>
          <%= game.name %>
        <% end %> - <small><%= game.company.name %></small>
          - <%= link_to "📝", edit_game_path(game) %>
      </li>
    <% end %>
  </ul>
</div>

